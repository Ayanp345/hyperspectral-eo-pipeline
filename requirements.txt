# Core numerical / scientific stack (required for every stage except `deep`)
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
pandas>=2.0
matplotlib>=3.7
seaborn>=0.12
pyyaml>=6.0

# Deep-learning stage (`python main.py deep`) -- optional. The rest of the
# pipeline runs and has been tested without this installed; `main.py`
# detects its absence and skips the deep-learning stage with a clear message.
torch>=2.1

# Optional: only needed if you want to run the automated tests via pytest
# instead of `python tests/test_pipeline.py` directly.
pytest>=7.0
